# Copia este archivo como .env y rellena los valores reales.
# .env está en .gitignore — nunca se sube al repo.
#
# Las claves de producción viven en Render como variables de
# entorno del servicio. Este archivo solo documenta qué claves
# espera el código y sirve para desarrollo local.

OPENAI_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
